---
title: Introduction
description: Welcome to Virexa, a professional Discord moderation and management platform powered by a FastAPI dashboard.
navigation:
  icon: i-lucide-sparkles
seo:
  title: Getting Started with Virexa
  description: Learn what Virexa is, how it works, and how the FastAPI-powered dashboard, bot, and endpoints fit together.
---

:::callout{icon="i-lucide-info" color="primary"}
**Looking for an issue or typo?**  
If you're hosting these docs from a Git repository, wire up the "Edit this page" button to your repo so staff can propose improvements directly.
:::

**Virexa** is a professional-grade Discord moderation and management solution. It combines a FastAPI backend, a modern web dashboard, and a powerful Discord bot so you can manage large communities with confidence.

This documentation walks through how to set up Virexa, grant the right permissions, understand core features, and use the HTTP and WebSocket endpoints that power the dashboard experience.

---

## What Is Virexa?

> Virexa centralizes server management into a single dashboard, backed by a typed, FastAPI-based API.

At a high level, Virexa consists of:

1. **FastAPI Application**  
   Hosts the dashboard, REST API endpoints, and WebSocket connections used for real-time data (for example, live logs).

2. **Discord Bot**  
   Connects to your servers, listens for events, and executes moderation, logging, and automation actions based on your configuration.

3. **Database Layer**  
   Persists servers, users, logs, configuration, and events so that state is durable across restarts and updates.

4. **Web Dashboard**  
   A SaaS-inspired UI for configuring moderation, logging, backups, role behavior, and other core features without memorizing commands.

---

## Why Use Virexa Instead of a Simple Bot?

1. **Dashboard-First Workflow**  
   No guessing which commands to run. Admins log into the dashboard, pick a server, and adjust configuration with forms, toggles, and checklists.

2. **FastAPI Architecture**  
   The backend is built on FastAPI, making routes predictable, typed, and easy to extend. You can integrate with the same endpoints that power the dashboard.

3. **Structured Moderation**  
   Moderation events are case-tracked and logged, making it easy to audit who did what, when, and why.

4. **Deep Logging & Analytics**  
   Rich logs and events are stored in the database and streamed in real time to the UI so you can spot patterns and issues early.

5. **Scales with Your Server**  
   Whether you're running a small community or a very large server, Virexa's architecture and database-backed design are built to scale.

---

## Core Capabilities at a Glance

- **Moderation System**: Case IDs, escalation rules, and structured staff workflows.  
- **Backup & Restore**: Protects your server configuration from accidents or malicious changes.  
- **Advanced Logging**: Tracks key Discord events for transparency and audits.  
- **Role Management**: Auto-roles, sticky roles, and bulk operations for large communities.  
- **AFK & Reminders**: Keeps important information visible and follow-ups from being forgotten.  
- **Boards & Counters**: Highlight community activity and server growth visually.  
- **FastAPI Endpoints**: Clean REST and WebSocket interfaces that can also power custom integrations.

For a feature-by-feature breakdown, see the [Core Features overview](/en/core-features/overview).

---

## How the Dashboard & API Work Together

The dashboard is just a client of the same API surface that you can integrate with:

- `GET /api/me` — Returns the currently authenticated Discord user.  
- `GET /api/guilds` — Lists guilds where the user has administrator permissions.  
- `GET /api/logs` — Fetches recent log entries for use in tables and charts.  
- `WEBSOCKET /ws/logs` — Streams logs in real time to the dashboard.  
- `POST /settings` — Saves server-specific configuration (prefix, log channels, enabled events).

FastAPI also exposes interactive API docs at `/api/docs` and `/api/redoc`, but this site is the **primary** curated documentation for how Virexa is intended to be used.

---

## Ready to Start?

Getting Virexa running for your team is straightforward.

:::steps

### Step 1: Deploy or Run the FastAPI App

Run the dashboard application (typically via `uvicorn` in development) and ensure it is reachable by your staff.  
By default, in local development, Virexa runs at:

```bash
http://localhost:8000
```

### Step 2: Invite the Bot

Invite the Virexa bot to your Discord server using your deployment’s invite link.  
Make sure you have the **Manage Server** permission in the target server.

### Step 3: Log In with Discord

Visit your dashboard URL (for example, `http://localhost:8000`) and authenticate using **Login with Discord**.  
Once logged in, select the server you want to configure.

### Step 4: Configure Basics

From the dashboard, set:

- Log channel and events to capture  
- Moderation preferences  
- Role behavior and hierarchy-sensitive actions

:::

---

## Where to Go Next

- **Setup Guide** → [Initial Configuration](/en/getting-started/setup)  
- **Permissions** → [Permission Requirements](/en/getting-started/permissions)  
- **Feature Overview** → [Core Features](/en/core-features/overview)

:::note
Need a deeper breakdown of endpoints and flows?  
Individual feature pages under **Core Features** expand on behavior, configuration, and how the dashboard talks to the API.
:::

